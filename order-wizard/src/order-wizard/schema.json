{
  "id": "orderWizardSchematic",
  "title": "Material Stepper Order Wizard",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component that shows up when help command typed"
    },
    "spec": {
      "type": "string",
      "description": "Specifies if a spec file is generated.",
      "default": "true"
    },
    "path": {
      "type": "string",
      "description": "Specifies path to create the order wizard under",
      "default": ""
    }
  },
  "required": [ "name" ]
}
